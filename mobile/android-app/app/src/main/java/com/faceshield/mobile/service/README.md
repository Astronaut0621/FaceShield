# Service Module

Future responsibilities:

- Run a foreground service while the floating window is active.
- Own overlay lifecycle.
- Own capture session lifecycle after MediaProjection authorization.
- Display a persistent notification required by Android for foreground work.
- Stop cleanly and remove overlay when the user disables protection.

