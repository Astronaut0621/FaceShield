# Detection Module

Future responsibilities:

- Coordinate capture, upload, backend detection, and result presentation.
- Debounce repeated floating-window taps.
- Track states such as idle, capturing, uploading, detecting, success, failure.
- Map backend risk fields to mobile result states.

