# Auth Module

Future responsibilities:

- Login with backend `/api/auth/login`.
- Persist Bearer token securely.
- Expose current user state to the mobile workflow.
- Clear token on logout or authentication failure.

